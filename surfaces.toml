# The surfaces seo-kit audits. Add LinkedIn / GitHub profile / products here as
# they come online. `gsc_property` uses Search Console's identifier form
# (`sc-domain:<domain>` for a Domain property; a full URL for a URL-prefix one).
#
# GEO probe config per surface: surface_markers (strings that prove an answer is
# about THIS entity), namesake_markers (the namesake to disambiguate from),
# geo_probes (the questions), cite_domains (what counts as a citation).
# geo_probe skips a surface without these.
